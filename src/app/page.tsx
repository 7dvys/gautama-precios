import { Metadata } from 'next'
export const metadata:Metadata = {
    title: 'ZPL printer',
    description: 'Genera etiquetas con ZPL',
}

const Home = ()=>{
    return(
        <>inicio</>
    )}
export default Home;