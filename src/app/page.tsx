import { Metadata } from 'next'
export const metadata:Metadata = {
    title: 'ZPL Precios',
    description: 'Controla tus precios!!',
}

const Home:React.FC = ()=>{
    return(
        <></>
    )}
export default Home;